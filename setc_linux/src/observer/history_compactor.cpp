#include "history_compactor.hpp"

namespace omnivggt::observer {
// The compaction operation is implemented by VersionStore so it can rewrite
// the existing binary indexes without introducing a second storage format.
}
